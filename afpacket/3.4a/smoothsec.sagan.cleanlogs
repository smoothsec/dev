#!/bin/bash

if [ -f /etc/smoothsec.cfg ]; then
	. /etc/smoothsec.cfg
fi

. /etc/afpacket.cfg

if [ -d /var/log/sagan ]; then
	/usr/bin/find  /var/log/sagan/processed/ -name "sagan.u2*"  -mtime +$DAYS -exec rm {} \;

else 
	echo "Sagan directory doesn't exist!!!"

fi
