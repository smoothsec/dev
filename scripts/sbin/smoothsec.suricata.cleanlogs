#!/bin/bash

. /etc/smoothsec.cfg

if [ -d /var/log/suricata ]; then

	/usr/bin/find  /var/log/suricata/processed/ -name "unified2.*"  -mtime +$DAYS -exec rm {} \;

else 

	echo "Suricata directory doesn't exist!!!"

fi
